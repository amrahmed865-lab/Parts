name: Check Reminders

on:
  schedule:
    - cron: '*/5 * * * *'   # كل 30 دقيقة
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Call Reminder API
        run: |
          curl -L https://vt.tiktok.com/ZSQWyd27h/
